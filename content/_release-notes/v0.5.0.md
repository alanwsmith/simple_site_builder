### Version 0.5.0

- The content tree is moved
to a `.build-files` directory 
and the build occurs from those
files.

- Scripts inside the `scripts/file-prep`
directory are run over txt files
during the move to the `.build-files`
directory to do things like 
find and replace or any other
pre-processing work that needs
to be done. 

