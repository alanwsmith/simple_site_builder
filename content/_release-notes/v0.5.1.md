### Version 0.5.1

- TODO: Added a `config/find-replace.txt`
file that gets picked up and does
simple find/replace strings.

- Removed `scripts/file-prep` it's
too slow on a moderately sized site. 

- Removed `scripts` dir all together 
in favor of `config` dir. Probably
a scripts dir will be added there. 
