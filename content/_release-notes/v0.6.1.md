### Version 0.6.1

Oct. 4, 2025

- Added highlight_file("file_path") function to 
replace original highlight_file["file_path"]

- Added markdown_file("file_path") function to replace 
original markdown_file["file_path"]

-  Moved `config/find-replace.txt` to `support/find-replace/*.txt`
directory that supports multiple text files. 


