# Scripts


TODO: ".html", ".js", ".json", ".txt", ".md"
files are send through the
scripts in the `pre_file` directory
before they are loaded into the 
minijinja engine. Find and Replace
stuff can be done here or anything.

TODO: Anything in the "pre_build" directory
gets run before the build process

TODO: Anything in the "post_build" directory
gets run after the build process

## Processing Order

- prelude 

- file-prep 

- cleanup 

